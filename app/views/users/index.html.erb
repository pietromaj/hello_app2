<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>
 <div class="panel panel-default sombra pad">
  <ul class="users">
    <%= render @users %>
  </ul>
</div>  

<%= will_paginate %>
